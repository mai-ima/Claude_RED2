# バックログ(未着手・将来対応メモ)

このファイルは今後対応予定のアイデアを残す内部メモ。`.vercelignore` で配信対象外。

## 方針: 過去データはコード内にアーカイブとして残す

コンテンツを大きく書き換える(例: 本文の大幅加筆、仕様の刷新)際は、**旧バージョンを
削除せずコード内にアーカイブとして保存する**。配信・表示には使わず、参照・復元用に残す。

- 実例: ニュース本文の加筆前(短い版)を `scripts/data_misc.py` の `NEWS_BODY_ARCHIVE`
  (id → {excerpt, body})として保存済み。gen.py からは未参照=非配信。
- 今後同種の書き換えを行う場合も、`*_ARCHIVE` 等の命名で旧データを併存させる。

## 第1弾コラボに「独自の冷却技術」を追加(将来)
- 対象: 第1弾コラボ4機種(七耀 / 残響 / 夜行 / 前線)。
- 内容: 各作品専用の冷却技術を新規に用意し、コラボLP・製品スペック・`/tech/cooling/` 系に反映する。
  第1弾コラボは筐体・SoC(元素炉/共振/夜想/基幹)まで専用設計なので、冷却も作品専用にして
  「色替えではない」思想を徹底する。
- 方向性の案(名称は要検討・作品トーンに合わせる):
  - 七耀(原神): 元素をモチーフにした冷却(例: 元素循環ベイパー)。
  - 残響(鳴潮): 静音・共振を活かした冷却(既存 LP に「共振鎖冷却」の語あり → これを技術ページ化)。
  - 夜行(NTE): 夜間・低照度撮影の発熱対策に振った冷却。
  - 前線(エンドフィールド): 堅牢・広温度域(-20℃〜)で効く現場向け冷却。
- 実装メモ: `scripts/data_tech.py`(冷却)/`scripts/data_collab.py`(highlights・silicon)/
  各コラボ LP ビルダー(`gen.py` の `_collab_lp_*`)に散在。単一ソースで足せる形にする。
- ステータス: **未着手(ユーザー指示で将来対応)。今回サイクルでは実装しない。**
